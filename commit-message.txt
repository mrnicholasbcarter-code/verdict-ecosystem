feat(BOD-204): adopt OpenSpec with verdict-change-v1 schema + deterministic validator

Adopt Fission-AI/OpenSpec as Verdict's executable significant-change contract
with a custom verdict-change-v1 schema that preserves all BOD-167 Spec Kit Lite
required fields.

OpenSpec provides proposal → specs → design → tasks lifecycle and tooling while
verdict-change-v1 ensures the BOD-167 contract fields are enforced.

Changes:
- Initialize OpenSpec 1.13.2 with --tools none (repo-local only, no telemetry)
- Fork spec-driven schema to verdict-change-v1 with BOD-167 field mapping
- Add deterministic validator scripts/validate_openspec_change.py (stdlib only)
- Add sample valid change openspec/changes/bod-204-openspec-foundation/
- Add malformed test fixture tests/fixtures/openspec/malformed-change/
- Add tests/test_openspec_change.py with 9 tests (all pass)
- Add docs/OPENSPEC.md with authority boundaries, triggers, field mapping, commands
- Link OPENSPEC.md from README.md
- Add superseded note to docs/SPEC_KIT_LITE.md

Authority boundaries preserved:
- Linear: planning/priority/state
- OpenSpec: change contract structure
- Git/GitHub: source/PR/merge truth
- Verdict: plan/admit/execute/verify/review/receipt
- Prime Agent: execution harness (NOT an OpenSpec authority)

BOD-167 field mapping complete:
All 14 required fields mapped to verdict-change-v1 artifacts (proposal/specs/design/tasks).
See docs/OPENSPEC.md for detailed mapping table.

Validation:
- scripts/validate_openspec_change.py passes on sample, fails on malformed
- All 9 unittest tests pass
- All CI checks pass (check_local_links, unittest, check_adr_lifecycle,
  verify_adr_sources, check_ownership_phrases, secret_scan, inventory_check,
  git diff --check)

No mass migration:
Historical SPEC_KIT_LITE.md, specs/, .specify/, and ADRs remain unchanged.
OpenSpec applies to new significant changes only.

OpenSpec files generated by init (all repo-local):
- openspec/config.yaml (customized with verdict-change-v1 and project context)
- openspec/changes/archive/.gitkeep
- openspec/specs/.gitkeep
- openspec/schemas/verdict-change-v1/ (forked from spec-driven)

References:
- BOD-204: https://linear.app/bodanglin/issue/BOD-204
- BOD-167: https://linear.app/bodanglin/issue/BOD-167
- OpenSpec: https://github.com/Fission-AI/OpenSpec
